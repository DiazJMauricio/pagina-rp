import React from "react";
import "./App.css";
import Page from "./components/Page";
import { HashRouter } from "react-router-dom";

export default () => (
  <HashRouter>
    <Page />
  </HashRouter>
);
